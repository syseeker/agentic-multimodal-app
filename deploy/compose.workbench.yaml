# Phase 8 — Sherlock Case Workbench
# FastAPI backend (:8200) + Vite-built Svelte SPA served as static files
# docker compose -p amms -f deploy/compose.workbench.yaml up -d
services:
  workbench:
    build:
      context: ../ui
      dockerfile: Dockerfile
    image: amms-workbench:latest
    container_name: amms-workbench
    working_dir: /app
    volumes:
      - ..:/app:ro
      - ../data/cases:/app/data/cases   # writable — evidence upload writes here
      - ../.env:/app/.env:ro
    environment:
      AIQ_URL: http://amms-aiq-agent:8000
      NEO4J_URI: bolt://amms-neo4j:7687
      NEO4J_USER: neo4j
      NEO4J_PASS: sherlock_dev
      # VSS services run with network_mode: host — reach via host-gateway (Docker resolves to host IP on any machine)
      VIOS_URL: http://host.docker.internal:30888
      VSS_AGENT_URL: http://host.docker.internal:8000
      INGESTOR_URL: http://ingestor-server:8082
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8200:8200"
    networks:
      - amms_aiq-network
      - amms_default
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8200/api/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s

networks:
  amms_aiq-network:
    external: true
  amms_default:
    external: true
